package com.uber.myapplication;

import androidx.annotation.NonNull;

public class TestGoogleJavaFormatUtil {

  @NonNull private Object onAttachInitialisedField;
  @NonNull private Object mOnCreateViewInitialisedField;
  @NonNull private Object mOnCreateInitialisedField;

  private int test(
      int a,
      int b,
      int c,
      int d,
      String message1,
      String message2,
      String message3,
      String message4) {

    // This is just comment with several blanket line above before the commit.
    throw new RuntimeException();

    // This is just another comment with several blanket line above before the commit.

  }
}
